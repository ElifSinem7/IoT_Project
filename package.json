{
  "name": "know-the-air-monorepo",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && .venv\\Scripts\\activate && uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "dev:frontend": "cd frontend && npm run dev"
  }
}
